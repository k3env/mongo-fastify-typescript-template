# Mongo Fastify TypeScript (MFT) template

REST API Template built with...:

- TypeScript for type definitions
- Mongo as database
- Fastify as web server

Also contains Dockerfile for packaging into docker image (compatible with kaniko)
